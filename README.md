# 🔍 Поисковый движок (Search Engine)

Веб-приложение для индексации сайтов и полнотекстового поиска по проиндексированным страницам. Реализовано как Spring Boot приложение с REST API и веб-интерфейсом.

## 📋 Описание

Поисковый движок позволяет:
- Автоматически обходить и индексировать указанные сайты
- Выполнять полнотекстовый поиск по проиндексированным страницам
- Получать статистику по индексированным сайтам и страницам
- Управлять процессом индексации через API

## ✨ Основные возможности

- 🔄 **Автоматическая индексация** - обход всех страниц указанных сайтов
- 🔎 **Полнотекстовый поиск** - поиск с учетом морфологии русского и английского языков
- 📊 **Статистика** - информация о проиндексированных сайтах и страницах
- 🌐 **Веб-интерфейс** - удобный пользовательский интерфейс для управления
- 🚀 **REST API** - программный доступ ко всем функциям
- ⚡ **Ранжирование результатов** - сортировка результатов по релевантности

## 🛠 Технологический стек

- **Java 17**
- **Spring Boot 3.5.5**
- **PostgreSQL** (в Docker контейнере)
- **Spring Data JPA**
- **Flyway** (миграции БД)
- **JSoup** (парсинг HTML)
- **Apache Lucene Morphology** (лемматизация для русского и английского языков)
- **Thymeleaf** (веб-интерфейс)
- **Lombok**

## 📦 Требования

- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose
- PostgreSQL 17 (запускается в Docker)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd search-engine
```

### 2. Запуск базы данных PostgreSQL в Docker

```bash
docker run --rm --name pg-docker \
  -e POSTGRES_PASSWORD=pwd \
  -e POSTGRES_USER=usr \
  -e POSTGRES_DB=demoDB \
  -p 5430:5432 \
  postgres:17
```

### 3. Настройка конфигурации

Отредактируйте файл `application.yml` и укажите сайты для индексации:

```yaml
indexing-settings:
  sites:
    - url: https://example.com/
      name: Пример сайта
    - url: https://another-site.com/
      name: Другой сайт
```

### 4. Сборка проекта

```bash
mvn clean install
```

### 5. Запуск приложения

```bash
mvn spring-boot:run
```

Или запустите собранный JAR файл:

```bash
java -jar target/SearchEngine-1.0-SNAPSHOT.jar
```

Приложение будет доступно по адресу: `http://localhost:8080`

## ⚙️ Конфигурация

Основные настройки находятся в файле `application.yml`:

```yaml
server:
  port: 8080  # Порт приложения

spring:
  datasource:
    username: usr          # Имя пользователя БД
    password: pwd          # Пароль БД
    url: jdbc:postgresql://localhost:5430/demoDB  # URL подключения к БД

indexing-settings:
  sites:                   # Список сайтов для индексации
    - url: https://example.com/
      name: Название сайта

connection-settings:
  userAgent: Mozilla/5.0...  # User-Agent для HTTP запросов
  referer: https://www.google.com  # Referer для HTTP запросов
```

## 📡 API Endpoints

### Статистика

**GET** `/api/statistics`

Возвращает статистику по всем проиндексированным сайтам и страницам.

**Ответ:**
```json
{
  "result": true,
  "statistics": {
    "total": {
      "sites": 2,
      "pages": 150,
      "lemmas": 5000,
      "isIndexing": false
    },
    "detailed": [
      {
        "url": "https://example.com/",
        "name": "Пример сайта",
        "status": "INDEXED",
        "statusTime": "2024-01-01T12:00:00",
        "error": null,
        "pages": 100,
        "lemmas": 3000
      }
    ]
  }
}
```

### Запуск индексации

**GET** `/api/startIndexing`

Запускает полную индексацию всех сайтов, указанных в конфигурации.

**Ответ:**
```json
{
  "result": true
}
```

**Ошибки:**
- `409 Conflict` - индексация уже запущена

### Остановка индексации

**GET** `/api/stopIndexing`

Останавливает текущий процесс индексации.

**Ответ:**
```json
{
  "result": true
}
```

**Ошибки:**
- `405 Method Not Allowed` - индексация не запущена

### Индексация отдельной страницы

**POST** `/api/indexPage`

Индексирует или обновляет индекс для указанной страницы.

**Параметры:**
- `url` (обязательный) - URL страницы для индексации

**Пример:**
```bash
curl -X POST "http://localhost:8080/api/indexPage?url=https://example.com/page"
```

**Ответ:**
```json
{
  "result": true
}
```

**Ошибки:**
- `400 Bad Request` - страница находится за пределами сайтов, указанных в конфигурации

### Поиск

**GET** `/api/search`

Выполняет поиск по проиндексированным страницам.

**Параметры:**
- `query` (обязательный) - поисковый запрос
- `site` (опциональный) - ограничение поиска конкретным сайтом (URL)
- `offset` (опциональный, по умолчанию 0) - смещение для пагинации
- `limit` (опциональный, по умолчанию 20) - количество результатов на странице

**Пример:**
```bash
curl "http://localhost:8080/api/search?query=java+spring&site=https://example.com&offset=0&limit=10"
```

**Ответ:**
```json
{
  "result": true,
  "count": 5,
  "data": [
    {
      "site": "https://example.com/",
      "siteName": "Пример сайта",
      "uri": "/page",
      "title": "Заголовок страницы",
      "snippet": "Фрагмент текста с <b>выделенными</b> словами запроса...",
      "relevance": 0.95
    }
  ]
}
```

**Ошибки:**
- `400 Bad Request` - задан пустой поисковый запрос

## 🗄 Структура базы данных

Приложение использует следующие таблицы:

- **site** - информация о сайтах (URL, статус индексации, время последнего обновления)
- **page** - проиндексированные страницы (путь, код ответа, содержимое)
- **lemma** - леммы (нормализованные слова) с частотой встречаемости
- **search_index** - индекс связи страниц и лемм с рангами релевантности

Миграции базы данных находятся в `src/main/resources/db/migration/` и выполняются автоматически при запуске приложения через Flyway.

## 📁 Структура проекта

```
search-engine/
├── src/
│   ├── main/
│   │   ├── java/searchengine/
│   │   │   ├── config/          # Конфигурационные классы
│   │   │   ├── controllers/     # REST контроллеры
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── model/           # JPA сущности
│   │   │   ├── repository/      # JPA репозитории
│   │   │   ├── services/        # Бизнес-логика
│   │   │   └── util/            # Утилиты
│   │   └── resources/
│   │       ├── db/migration/    # SQL миграции Flyway
│   │       ├── static/          # Статические ресурсы (CSS, JS)
│   │       └── templates/       # Thymeleaf шаблоны
│   └── test/
├── application.yml              # Конфигурация приложения
├── pom.xml                      # Maven зависимости
└── README.md                    # Документация
```

## 🔍 Принципы работы

1. **Индексация сайтов:**
   - Приложение обходит все страницы указанных сайтов
   - Извлекает текст из HTML с помощью JSoup
   - Разбивает текст на слова и приводит их к базовой форме (лемматизация)
   - Сохраняет леммы, страницы и связи между ними в базе данных

2. **Поиск:**
   - Поисковый запрос разбивается на слова
   - Слова приводятся к базовой форме
   - Находятся страницы, содержащие все слова запроса
   - Результаты ранжируются по релевантности и сортируются

3. **Ранжирование:**
   - Используется TF-IDF алгоритм для расчета релевантности
   - Учитывается частота встречаемости слов на странице и в индексе

## 🌐 Веб-интерфейс

Веб-интерфейс доступен по адресу `http://localhost:8080` и предоставляет:
- Форму для поиска
- Статистику по индексированным сайтам
- Управление процессом индексации

## 📝 Лицензия

Данный проект является финальным заданием курса.

## 👤 Автор: Волков Алексей Сергеевич

Проект разработан в рамках обучения.

---

**Примечание:** Убедитесь, что база данных PostgreSQL запущена перед запуском приложения. При первом запуске Flyway автоматически создаст необходимые таблицы.
